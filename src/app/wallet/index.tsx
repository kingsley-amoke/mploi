import { StyleSheet, View } from 'react-native'
import React from 'react'
import { Text } from 'react-native-paper'

const index = () => {
  return (
    <View>
      <Text>Wallet goes here</Text>
    </View>
  )
}

export default index

const styles = StyleSheet.create({})